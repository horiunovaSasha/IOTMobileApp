<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="IOTMobileApp.Views.AlarmDetailsPage"
             Title="Новий будильник">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Скасувати" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Зберегти" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Змінити час" FontSize="Medium" />
            <!--<Entry Text="{Binding Item.Time}" d:Text="Час" FontSize="Small" />-->
            <TimePicker Time="{Binding Alarm.Time}" />
            <!--<Label Text="Description" FontSize="Medium" />
            <Editor Text="{Binding Item.Description}" d:Text="Item description" FontSize="Small" Margin="0" />-->
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
